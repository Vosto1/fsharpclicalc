{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "msbuild",
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build",
            "label": "build: solution",
            "detail": "build the Calculator.sln solution using dotnet msbuild /t:build"
        }
    ]
}